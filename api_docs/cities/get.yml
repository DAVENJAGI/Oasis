get:
  tags:
    - Cities
  summary: Get all cities
  description: Retrieve a list of all city objects.
  responses:
    200:
      description: List of all cities
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/City'

