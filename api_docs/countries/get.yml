get:
  tags:
    - Countries
  summary: Get all countries
  description: Retrieve a list of all country objects.
  responses:
    200:
      description: List of countries
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/Country'

