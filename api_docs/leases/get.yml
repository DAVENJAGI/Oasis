tags:
  - Lease
summary: Get all leases
description: Retrieve all lease records. Only accessible by an admin.
responses:
  200:
    description: A list of all lease records
    schema:
      type: array
      items:
        $ref: '#/definitions/Lease'
  401:
    description: Unauthorized access

