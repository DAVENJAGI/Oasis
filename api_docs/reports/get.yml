tags:
  - Report
summary: Get all reports
description: Retrieve all report objects (admin only).
responses:
  200:
    description: A list of reports
    schema:
      type: array
      items:
        $ref: '#/definitions/Report'
  403:
    description: Unauthorized access

