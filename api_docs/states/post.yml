post:
  tags:
    - States
  summary: Create a new state
  description: Add a new state object.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/StateInput'
  responses:
    201:
      description: State created successfully
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/State'
    400:
      description: Missing name or not a JSON

